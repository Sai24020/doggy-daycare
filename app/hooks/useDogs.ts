/*import { DogsContext } from "@/app/context/dogs-context"
import { useContext } from "react"

// 5. anpassad hook för att använda kontexten
export const useDogsContext = () => {
    const context = useContext(DogsContext);
    if (!context) {
        throw new Error("useDogsContext måste användas inom en DogsProvider")
    }
    return context;
}*/